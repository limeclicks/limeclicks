# Environment variables to force headless mode for audits

# Force headless Chrome for Lighthouse
CHROME_PATH=/usr/bin/google-chrome
CHROME_FLAGS="--headless=new --no-sandbox --disable-gpu --disable-dev-shm-usage"

# Force headless mode for all processes
DISPLAY=:99
HEADLESS=true
NO_GUI=true

# Disable browser auto-open
BROWSER=none